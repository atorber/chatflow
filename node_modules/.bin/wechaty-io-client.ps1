#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/-S$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/-S$exe" node --no-warnings --loader ts-node/esm "$basedir/../wechaty/dist/esm/bin/io-client.js" $args
  } else {
    & "$basedir/-S$exe" node --no-warnings --loader ts-node/esm "$basedir/../wechaty/dist/esm/bin/io-client.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "-S$exe" node --no-warnings --loader ts-node/esm "$basedir/../wechaty/dist/esm/bin/io-client.js" $args
  } else {
    & "-S$exe" node --no-warnings --loader ts-node/esm "$basedir/../wechaty/dist/esm/bin/io-client.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
