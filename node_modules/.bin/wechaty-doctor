#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../wechaty/dist/esm/bin/doctor.js" "$@"
else 
  exec node  "$basedir/../wechaty/dist/esm/bin/doctor.js" "$@"
fi
